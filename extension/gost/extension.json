{
  "bin": "./extension/gost/bin/gost",
  "args": "-L=:{{ ExtensionPort }}{% if ServerPort %} -F={{ Protocol }}://{{ ServerAddress }}:{{ ServerPort }}{% if Bypass %}?bypass=./profile/{{ Bypass }}{% endif %}{% endif %} {{ Other }}",
  "exitargs":"",
  "keys": ["Protocol", "ServerAddress", "ServerPort", "Bypass", "Other"],
  "http": true,
  "socks": true,
  "render": {},
  "default": {
    "Protocol": "socks5",
    "ServerAddress": "127.0.0.1"
  }
}